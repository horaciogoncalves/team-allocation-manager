# Neon PostgreSQL connection string
# Server-side only. Never expose this in the browser or client bundle.
# Get it from your Neon project dashboard: Connection Details → PostgreSQL connection string
DATABASE_URL=postgresql://username:password@host.neon.tech/database?sslmode=require
